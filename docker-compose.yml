version: '3.8'

services:
  fastapi-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src                # ✅ Mount only your source code
      - ./src/mlruns:/app/src/mlruns  # ✅ Mount MLflow model artifacts
    environment:
      - PYTHONUNBUFFERED=1
